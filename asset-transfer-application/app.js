'use strict';

const { Gateway, Wallets } = require('fabric-network');
const FabricCAServices = require('fabric-ca-client');
const fs = require('fs');
const path = require('path');

const main = async () => {
    try{
        const ccpPath = path.resolve(__dirname, '..', '..', 'fabric-netwrok', 'build', 'crypto-config', 'peerOrganizations', 'ca_ai.islab.re.kr', )

        try{

        } finally {

        }

    }catch (error) {
        console.error(`Failed to run app: ${error}`);
    }
}